# Required for Gemini AI calls (server-side)
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Candidates Supabase project (stores users, candidates, applications)
# Service role key — server-side ONLY, never expose to the browser
SUPABASE_URL="https://iuleoikrcxpvtajekksy.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Jobs Supabase project (read-only — fetches the jobs table)
JOBS_SUPABASE_URL="https://owsbrhyzkprqgasesbqa.supabase.co"
JOBS_SUPABASE_ANON_KEY="your-anon-key"

# Optional: app URL for self-referential links
APP_URL="MY_APP_URL"
